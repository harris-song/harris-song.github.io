---
title: "Page Not Found"
sitemap: false
permalink: /404.html
---

<div id="not-found-msg">
Sorry, but the page you were trying to view does not exist.
</div>

<div id="r-viewer">
  <p>Loading document…</p>
  <noscript>
    JavaScript is required to view the embedded document. Please open the file directly.
  </noscript>
  <div id="r-viewer-container"></div>
</div>

<script>
  (function () {
    try {
      var path = location.pathname || '';
      if (path.indexOf('/r/') === 0 && path.length > 3) {
        var rest = path.slice(3); // after '/r/'
        var target = '/files/' + rest;
        // Default to .pdf if no extension
        if (!/\.[a-z0-9]+$/i.test(target)) target += '.pdf';

        // Update title for clarity
        try {
          var base = rest.replace(/\/+$/,'').split('/').pop();
          if (base && !/\.[a-z0-9]+$/i.test(base)) base += '.pdf';
          document.title = (base ? base + ' - ' : '') + document.title;
        } catch (_) {}

        // Build embedded viewer
        var container = document.getElementById('r-viewer-container');
        var wrapper = document.getElementById('r-viewer');
        if (container && wrapper) {
          var html = '' +
            '<object data="' + target + '" type="application/pdf" width="100%" height="900px">' +
            '  <iframe src="' + target + '" width="100%" height="900px"></iframe>' +
            '  <p>Cannot embed PDF. <a href="' + target + '" target="_blank" rel="noopener">Open the file</a>.</p>' +
            '</object>' +
            '<p style="margin-top:8px;margin-bottom:0;"><a href="' + target + '" target="_blank" rel="noopener">Open in new tab</a></p>';
          container.innerHTML = html;
          // Hide default 404 message if present
          var notFound = document.getElementById('not-found-msg');
          if (notFound) notFound.style.display = 'none';
          // Hide the page title/header if present
          var titleEl = document.querySelector('.page__title') || document.querySelector('h1');
          if (titleEl) titleEl.style.display = 'none';
          // Hide the loading text inside the viewer wrapper
          var loadingEl = wrapper.querySelector('p');
          if (loadingEl) loadingEl.style.display = 'none';
        }
      }
    } catch (e) { /* no-op */ }
  })();
</script>
